# REHOSTING

Files can be found here: [b01lers CTF 2026](https://ctftime.org/event/3153)

## Challenge Setup
The handout `bike` is a reverse-engineering binary that validates a bitstream-encoded input against an embedded prefix tree.
The original challenge yields a static flag (solved offline from the artifacts), so this rehost adds `flagCheck` and `.flag.sha256`.
