`out.txt` contains the output of a custom keystream construction applied to thousands of random samples and the flag.

Solve the challenge to recover the original AmateursCTF flag, then run `/challenge/flagCheck` and enter that flag to get the pwn.college flag.
