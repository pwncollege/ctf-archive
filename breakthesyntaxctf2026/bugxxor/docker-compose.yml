services:
  web:
    build:
      context: .
    ports:
      - "8000:8000"
    restart: always
    # The app reads the flag from /flag. The image ships a default test flag;
    # mount the real flag here at deploy time:
    # volumes:
    #   - ./flag:/flag:ro
