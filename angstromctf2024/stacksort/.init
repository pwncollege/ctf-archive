#!/bin/bash

# Attempt to create a symboliclink
ln -s /flag /challenge/flag.txt 2>/dev/null 

cp /challenge/stacksort /challenge/stacksorted-patched && patchelf --set-rpath /challenge /challenge/stacksorted-patched
