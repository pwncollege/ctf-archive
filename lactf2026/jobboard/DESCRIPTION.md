I heard you were looking for a job.

Too bad the one that gives you the secret flag is private.

Trigger the stored XSS, recover the original LA CTF flag from the admin-only job page, then run `/challenge/flagCheck` and enter that flag to get the pwn.college flag.

---
**Author:** r2uwu2
