A statically-linked, non-stripped AArch64 (ARM64) binary. All symbols are intact (`main`, `win`, flag-loading helpers), so static analysis quickly surfaces the vulnerable path. Trigger it to read the flag.

---
**Author:** Midnight Sun CTF 2026 Team
