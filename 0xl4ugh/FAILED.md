# Failed Rehosts

- `bitcoin`: archive only includes `WRITEUP.md` and `solve.py`; the original crypto service artifact is missing.
- `HashCashSlash`: archive only includes writeup and progress notes; the original service wrapper and deployment artifacts are missing.
- `dune`: OSINT challenge depends on off-platform accounts, source packages, and open-web breadcrumbs rather than a self-contained artifact set.
- `meeting`: challenge depends on Google Maps and external business-directory and email lookups rather than a reproducible self-contained artifact set.
- `zoro`: archived binary only validated in the original Ubuntu 16.04 service container, and a clean pwn.college rehost was not reproduced.
- `12`: archived binary was not solved or validated reproducibly from the provided artifact alone; existing material is incomplete analysis, not a verified rehost.
- `psycho`: archived binary was not validated with a reproducible no-`sudo` solve path, and the directory mostly contains ad hoc analysis artifacts.
- `pdfexe`: archived source was not validated with a working exploit path from the provided artifacts.
