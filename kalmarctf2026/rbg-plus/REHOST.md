# REHOSTING

Files can be found here: [KalmarCTF 2026](https://ctftime.org/event/2983)

## Challenge Setup
The handout `chall.py` is the RSA+SBG+LCG generator and `output.txt` holds its 13 published sums.
The original challenge yields a static flag (solved offline from the artifacts), so this rehost adds `flagCheck` and `.flag.sha256`.
